using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MarketResourse : MonoBehaviour
{
    //public double _elasticity { get; private set; }
    //public double _BalanceAmount { get; private set; }
    //public double _MAXPrice { get; private set; }
    //public MarketResourse(uint Amount, string name, double MaxPrice, double Elasticity, double BalanceAmount = 5000, bool isMarketable = true) : base(Amount, name, isMarketable)
    //{
    //    _name = name;
    //    _amount = Amount;
    //    _isMarketable = isMarketable;
    //    _elasticity = (-1) * Elasticity;
    //    _BalanceAmount = BalanceAmount;
    //    _MAXPrice = MaxPrice;
    //}
    //public double Price() => _MAXPrice + (_elasticity * _amount);

    //private void MarketRestore()
    //{
    //    if (_amount != _BalanceAmount)
    //    {
    //        _amount += (int)((_BalanceAmount - _amount) / 7)*Time.deltaTime;
    //        if ((int)(_BalanceAmount - _amount) <= 7 && (int)(_BalanceAmount - _amount) >= -7)
    //        {
    //            _amount = _BalanceAmount;
    //        }
    //    }
    //}
    //public static void MarketRestoreAll(MarketResourse[] marketResources)
    //{
    //    foreach (var marketResource in marketResources)
    //    {
    //        marketResource.MarketRestore();
    //    }
    //}
}
